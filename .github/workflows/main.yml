name: Tomcat-deploy
# Controls when the workflow will run
on: [push]
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: Generate the package
        run: mvn -B package --file pom.xml
      - name: Deploy to tomcat
        run: curl -v -u ${{ secrets.admin}}:${{ secrets.rimmi}} -T /home/runner/work/java-project/java-project/target/WebApp-pipeline.war 'http://13.233.16.20:8080//manager/text/deploy?path=/y&update=true'
